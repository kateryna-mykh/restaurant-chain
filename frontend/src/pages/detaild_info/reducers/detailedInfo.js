const initialState = {
    isLoading: false,
    isFailed: false,
    //fields
};

export default function Reducer(state = initialState, action) {
    switch (action.type) {
        default: { return state; }
    }
}